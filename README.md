You can run the bot by following the following steps:

Run the following commands in your console:
git clone https://github.com/idkennnn/DiscordBot.git
cd DiscordBot
npm install

After that, create a .env file that contains the following:
TOKEN=The token of your bot

Finally, run this in your console and the bot will work with the established commands:
node index.js
